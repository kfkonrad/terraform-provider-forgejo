---
page_title: "Resource forgejo_team_membership - forgejo"
subcategory: ""
description: |-
  Forgejo team membership resource. Manages membership of users in teams.
---

# Resource (forgejo_team_membership)

Forgejo team membership resource. Manages membership of users in teams.

## Example Usage

```terraform
terraform {
  required_providers {
    forgejo = {
      source = "kfkonrad/forgejo"
    }
  }
}

provider "forgejo" {
  host = "http://localhost:3000"
}

# Organization
resource "forgejo_organization" "test" {
  name = "test_org"
}

# Team
resource "forgejo_team" "developers" {
  organization = forgejo_organization.test.name
  name         = "Developers"
  is_admin     = false

  permissions = {
    code   = "write"
    issues = "write"
    pulls  = "write"
  }
}

# User
resource "forgejo_user" "developer" {
  username = "developer1"
  email    = "developer1@example.com"
  password = "dev123pass"
}

# Team membership
resource "forgejo_team_membership" "dev_member" {
  team_id  = forgejo_team.developers.id
  username = forgejo_user.developer.username
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `team_id` (Number) Numeric identifier of the team.
- `username` (String) Username of the user to add to the team.

## Import

Team membership can be imported using the organization name, team name, and username separated by colons. Format: `organization:team_name:username`

```shell
terraform import forgejo_team_membership.example my-org:developers:john
```
